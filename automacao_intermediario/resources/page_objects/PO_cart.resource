*** Variables ***
${CART_TITLE}  //span[@data-test='title'][contains(text(), 'Your Cart')]

${CART_BTN_CHECKOUT}  id=checkout

${CART_FIELD_FIRST_NAME}  id=first-name

${CART_FIELD_LAST_NAME}  id=last-name

${CART_FIELD_POSTAL_CODE}  id=postal-code

${CART_BTN_CONTINUE}  id=continue