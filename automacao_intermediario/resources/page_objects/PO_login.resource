*** Settings ***
Documentation  Este arquivo contém as variáveis de configuração e elementos da página de login do sistema de vendas (https://www.saucedemo.com/).

*** Variables ***
${LOGIN_TITLE}  Swag Labs

${LOGIN_FIELD_USER}  id=user-name

${LOGIN_FIELD_PASSWORD}  id=password

${LOGIN_BUTTON}  id=login-button